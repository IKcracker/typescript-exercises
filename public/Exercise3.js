"use strict";
function sum(num1, num2) {
    return num1 + num2;
}
function pi() {
    const piValue = 3.1415926535;
    return piValue;
}
function returnTrue(word, optional) {
    if (optional) {
        return word.toUpperCase();
    }
    else {
        return word.toLowerCase();
    }
}
